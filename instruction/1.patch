--- a/src/features/Vault/layout/VaultDetailsPane.tsx
+++ b/src/features/Vault/layout/VaultDetailsPane.tsx
@@ -70,7 +70,13 @@
 
   return (
     <div className="vault-panel-wrapper">
-      <div className="vault-section-header">{tVault('information.title')}</div>
+      <div className="datacards-header">
+        <div className="vault-section-header">{tVault('information.title')}</div>
+
+        <div className="datacards-header__right">
+          <div className="datacards-header__spacer" aria-hidden="true" />
+        </div>
+      </div>
       <div className="vault-empty">{tDetails('empty.selectPrompt')}</div>
     </div>
   );
--- a/src/features/Vault/components/Details/Details.tsx
+++ b/src/features/Vault/components/Details/Details.tsx
@@ -131,7 +131,15 @@
     }));
   };
 
-  const informationTitle = <div className="vault-section-header">{tVault('information.title')}</div>;
+  const informationTitle = (
+    <div className="datacards-header">
+      <div className="vault-section-header">{tVault('information.title')}</div>
+
+      <div className="datacards-header__right">
+        <div className="datacards-header__spacer" aria-hidden="true" />
+      </div>
+    </div>
+  );
 
   if (!card) {
     return (
--- a/src/features/Vault/components/BankCards/BankCardDetails.tsx
+++ b/src/features/Vault/components/BankCards/BankCardDetails.tsx
@@ -133,7 +133,15 @@
     };
   }, []);
 
-  const informationTitle = <div className="vault-section-header">{tVault('information.title')}</div>;
+  const informationTitle = (
+    <div className="datacards-header">
+      <div className="vault-section-header">{tVault('information.title')}</div>
+
+      <div className="datacards-header__right">
+        <div className="datacards-header__spacer" aria-hidden="true" />
+      </div>
+    </div>
+  );
 
   if (!card) {
     return (
