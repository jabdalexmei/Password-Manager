diff --git a/src/app/App.tsx b/src/app/App.tsx
index 4db74b3..7d55069 100644
--- a/src/app/App.tsx
+++ b/src/app/App.tsx
@@ -99,9 +99,11 @@ const App: React.FC = () => {
 
   return (
     <ToasterProvider>
-      <Suspense fallback={<p className="muted centered" aria-busy="true">{t('label.loading')}</p>}>
-        {content}
-      </Suspense>
+      <div className="app-viewport">
+        <Suspense fallback={<p className="muted centered" aria-busy="true">{t('label.loading')}</p>}>
+          {content}
+        </Suspense>
+      </div>
     </ToasterProvider>
   );
 };
diff --git a/src/shared/styles/base.css b/src/shared/styles/base.css
index 93658ef..368fc31 100644
--- a/src/shared/styles/base.css
+++ b/src/shared/styles/base.css
@@ -3,6 +3,9 @@
   background-color: var(--sem-bg-app);
   font-family: var(--sem-font-body);
 
+  --app-design-width: 1920px;
+  --app-design-height: 1080px;
+
   --spacing-between-form-field-label-text-and-input-field: 6px;
   --spacing-between-navigation-field-label-text-and-input-field: 10px;
 }
@@ -42,10 +45,23 @@ body,
 #root {
   margin: 0;
   padding: 0;
+  width: 100%;
+  height: 100%;
   min-height: 100vh;
 }
 
 body {
   background: var(--sem-bg-app);
   color: var(--sem-text-primary);
+  overflow: hidden;
+}
+
+.app-viewport {
+  position: fixed;
+  inset: 0;
+  width: min(100vw, var(--app-design-width));
+  height: min(100vh, var(--app-design-height));
+  margin: auto;
+  overflow: hidden;
+  background: var(--sem-bg-app);
 }
diff --git a/src/shared/styles/screens/vault/header.css b/src/shared/styles/screens/vault/header.css
index 6868833..915c4b0 100644
--- a/src/shared/styles/screens/vault/header.css
+++ b/src/shared/styles/screens/vault/header.css
@@ -1,7 +1,9 @@
 ﻿.vault-shell {
+  width: 100%;
   display: flex;
   flex-direction: column;
-  height: 100vh;
+  height: 100%;
+  min-height: 0;
   background: var(--sem-surface-page);
   color: var(--sem-text-primary);
 }
diff --git a/src/shared/styles/ui/misc.css b/src/shared/styles/ui/misc.css
index 2ffd8cd..23d5e08 100644
--- a/src/shared/styles/ui/misc.css
+++ b/src/shared/styles/ui/misc.css
@@ -55,7 +55,9 @@
    - Screens can still override ONLY layout details (padding/width) via modifiers.
    ========================================================================== */
 .screen-shell {
-  min-height: 100vh;
+  width: 100%;
+  height: 100%;
+  min-height: 100%;
   display: flex;
   align-items: center;
   justify-content: center;
@@ -88,7 +90,7 @@
   padding-left: 24px;
   padding-right: 24px;
   padding-bottom: 20px;
-  max-height: calc(100vh - 64px);
+  max-height: calc(100% - 64px);
   overflow: hidden;
 }
 
diff --git a/src/shared/styles/ui/toasts.css b/src/shared/styles/ui/toasts.css
index 4f31a34..3edb841 100644
--- a/src/shared/styles/ui/toasts.css
+++ b/src/shared/styles/ui/toasts.css
@@ -5,8 +5,8 @@
    ========================================================================== */
 .toast-host {
   position: fixed;
-  right: 16px;
-  bottom: 16px;
+  right: max(16px, calc((100vw - var(--app-design-width)) / 2 + 16px));
+  bottom: max(16px, calc((100vh - var(--app-design-height)) / 2 + 16px));
   display: flex;
   flex-direction: column;
   gap: 10px;
